import React from 'react';

function MenuItem(props) {
    return (
        <div>
            Todo : Menu Items : Image + Item Titles + Description + PRice + Add button
        </div>
    );
}

export default MenuItem;